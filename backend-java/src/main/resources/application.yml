server:
  port: 8080

spring:
  application:
    name: backend-java

  datasource:
    url: jdbc:mysql://localhost:3306/enterprise_zoom
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  data:
    redis:
      host: localhost
      port: 6379